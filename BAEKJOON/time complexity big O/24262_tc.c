#include <stdio.h>

int main(void){
	
	/*
	해결해야 하는 조건
	
	big O 표기법 - 시간 복잡도
	
	MenOfPassion(A[], n) {
    i = ⌊n / 2⌋;
    return A[i]; # 코드1
	}
	
	#코드1의 수행 횟수는 n에 어떠한 값이 들어가더라도 항상 1이다.
	
	그렇기 때문에 수행 횟수는 항상 1로 일정하므로 -> 빅오 표기법 O(1) -> 최고차항은 항상 0이다.
	*/
	
	printf("1\n0\n");
	
	return 0;	
}